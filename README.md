# Shipping Platform

- Backend: `/backend` ASP.NET Core Web API + EF Core + MySQL + Azure Blob public URL storage.
- Frontend: `/frontend` React + TypeScript + Material UI admin panel.

See each folder README for run steps.
